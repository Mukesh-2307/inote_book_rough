import React from 'react'
// import {useContext} from 'react'
// import {useEffect} from 'react'
// import noteContext from '../context/notes/noteContext'

const About = () => {
  // const a = useContext(noteContext)
  // const {notesInitial} = a;
  // useEffect(() => {
  //   a.update()
  // // eslint-disable-next-line
  // }, [])
  
  return (
    <div>
      <h1>This page is about iNoteBook </h1> {/* if we are not passing the state "s1" directly the we need to access the name using the ".state" method} */}
          {/* if we are not passing the state "s1" directly the we need to access the name using the ".state" method} */}
    </div>
  )
}

export default About
 